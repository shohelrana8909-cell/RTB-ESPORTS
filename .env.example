VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Path to the admin login page. Real access control is Supabase Auth + RLS
# (see SUPABASE_SETUP.sql) — this obscure path is just an extra layer so
# nobody stumbles onto the login page. Change it before deploying.
VITE_ADMIN_PATH=/admin-x7k9
